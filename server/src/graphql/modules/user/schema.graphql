type User {
  id: ID!
  username: String!
  token: String!
}

type Mutation {
  createUser(data: CreateUserInput): User!
}

input CreateUserInput {
  username: String!
  password: String!
}
