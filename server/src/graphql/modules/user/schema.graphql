type User {
  id: ID!
  username: String!
  password: String
}

type Mutation {
  createUser(data: CreateUserInput): User!
}

input CreateUserInput {
  username: String!
  password: String!
}
